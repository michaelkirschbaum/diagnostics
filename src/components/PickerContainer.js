import React from 'react';
import { Container, Content, Picker } from 'native-base';
const Item = Picker.Item;

const PickerContainer = React.createClass({
  render() {
    
  }
});

export default PickerContainer;
